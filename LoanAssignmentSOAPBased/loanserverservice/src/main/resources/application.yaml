spring:
  datasource:
    driver-class:  org.postgresql.Driver
    url:  jdbc:postgresql://localhost:5432/training
    username:  postgres
    password:  postgres


  jpa:
    show-sql:  true
    hibernate:
      ddl-auto:  create

server:
  port:  8080